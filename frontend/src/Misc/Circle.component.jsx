export const Circle = ({ children, className }) => ( 
    <div class={` w-min h-min px-4 py-1 font-bold text-gray-700 rounded-full bg-white flex items-center justify-center font-mono ${className}`} >
        {children}
    </div>
 )


 